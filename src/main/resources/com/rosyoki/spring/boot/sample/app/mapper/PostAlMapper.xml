<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rosyoki.spring.boot.sample.app.mapper.PostAlMapper">
    <select id="select" resultType="com.rosyoki.spring.boot.sample.app.entity.PostZipData">
        SELECT id, city, old_zip, pref,town,zip FROM post_zip_data WHERE id = #{id}
    </select>
    <select id="selectByCity" resultType="map" parameterType="java.lang.String">
        SELECT id, city, old_zip, pref,town,zip FROM post_zip_data WHERE city = #{city}
    </select>
    <select id="selectByZip" resultType="com.rosyoki.spring.boot.sample.app.entity.PostZipData">
        SELECT id, city, old_zip, pref,town,zip FROM post_zip_data WHERE zip = #{zip}
    </select>
</mapper>